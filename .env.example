NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
# The single private space served at the root (production). This is a routing
# identifier, not the password; data access is protected by the session cookie.
NEXT_PUBLIC_SPACE_TOKEN=
